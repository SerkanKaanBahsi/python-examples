#Not so common use Lists instead 
import array

#Creating Arrays
# "i" = integer, "b" = char , "d" = double, "f"=float, "h" = short, "l"= long "u" = PY_UNICODE 
my_array=array.array('d',[2.2,0.005,12.5])
sec_array=array.array("i",[1,2,3,4,5,6])

#Rest is same with list so no need